import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lg-code-challenge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
